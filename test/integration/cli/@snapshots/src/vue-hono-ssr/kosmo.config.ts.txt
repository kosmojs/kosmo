import {
  defineConfig,
  vueGenerator,
  honoGenerator,
  ssrGenerator,
  fetchGenerator,
  typeboxGenerator,
} from "@kosmojs/dev";

export default defineConfig({
  base: "/vue-hono-ssr",
  generators: [
    vueGenerator(),
    honoGenerator(),
    ssrGenerator(),
    fetchGenerator(),
    typeboxGenerator(),
  ],
});
