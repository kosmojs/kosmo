import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "svelte",
    base: "/svelte-ssr",
    fetch: true,
    ssr: true,
    ssg: false,
    tanstack: { query: false },
  },
});
