import { defineConfig, mdxGenerator, ssrGenerator, ssgGenerator } from "@kosmojs/dev";
import frontmatterPlugin from "remark-frontmatter";
import mdxFrontmatterPlugin from "remark-mdx-frontmatter";

export default defineConfig({
  base: "/mdx-ssr-ssg-tsq",
  generators: [
    mdxGenerator({ remarkPlugins: [frontmatterPlugin, mdxFrontmatterPlugin] }),
    ssrGenerator(),
    ssgGenerator(),
  ],
});
