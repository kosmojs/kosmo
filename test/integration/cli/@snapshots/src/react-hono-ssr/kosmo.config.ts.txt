import {
  defineConfig,
  reactGenerator,
  honoGenerator,
  ssrGenerator,
  fetchGenerator,
  typeboxGenerator,
} from "@kosmojs/dev";

export default defineConfig({
  base: "/react-hono-ssr",
  generators: [
    reactGenerator(),
    honoGenerator(),
    ssrGenerator(),
    fetchGenerator(),
    typeboxGenerator(),
  ],
});
