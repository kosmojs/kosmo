import {
  defineConfig,
  vueGenerator,
  honoGenerator,
  ssrGenerator,
  ssgGenerator,
  fetchGenerator,
  typeboxGenerator,
} from "@kosmojs/dev";

export default defineConfig({
  base: "/vue-hono-ssg",
  generators: [
    vueGenerator(),
    honoGenerator(),
    ssrGenerator(),
    ssgGenerator(),
    fetchGenerator(),
    typeboxGenerator(),
  ],
});
