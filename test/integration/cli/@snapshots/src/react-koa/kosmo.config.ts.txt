import {
  defineConfig,
  reactGenerator,
  koaGenerator,
  fetchGenerator,
  typeboxGenerator,
} from "@kosmojs/dev";

export default defineConfig({
  base: "/react-koa",
  generators: [reactGenerator(), koaGenerator(), fetchGenerator(), typeboxGenerator()],
});
