import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "svelte",
    base: "/svelte-koa",
    ssr: true,
    ssg: false,
    tanstack: { query: false },
  },
  backend: {
    stack: "koa",
    base: "/svelte-koa/api",
  },
  fetch: true,
  validation: true,
  typecheck: true,
});
