import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "svelte",
    base: "/svelte-koa",
    fetch: true,
    ssr: false,
    ssg: false,
    tanstack: { query: false },
  },
  backend: {
    stack: "koa",
    base: "/svelte-koa/api",
  },
  validation: true,
});
