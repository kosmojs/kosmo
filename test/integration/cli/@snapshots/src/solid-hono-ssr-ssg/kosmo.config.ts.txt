import {
  defineConfig,
  solidGenerator,
  honoGenerator,
  ssrGenerator,
  ssgGenerator,
  fetchGenerator,
  typeboxGenerator,
} from "@kosmojs/dev";

export default defineConfig({
  base: "/solid-hono-ssr-ssg",
  generators: [
    solidGenerator(),
    honoGenerator(),
    ssrGenerator(),
    ssgGenerator(),
    fetchGenerator(),
    typeboxGenerator(),
  ],
});
