import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "mdx",
    base: "/mdx-h3-tsq",
    fetch: true,
    ssr: false,
    ssg: false,
  },
  backend: {
    stack: "h3",
    base: "/mdx-h3-tsq/api",
  },
  validation: true,
});
