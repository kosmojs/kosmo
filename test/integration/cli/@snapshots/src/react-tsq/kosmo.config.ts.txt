import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "react",
    base: "/react-tsq",
    fetch: true,
    ssr: false,
    ssg: false,
    tanstack: { query: true },
  },
});
