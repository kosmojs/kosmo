import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "react",
    base: "/react-koa-ssr-tsq",
    fetch: true,
    ssr: true,
    ssg: false,
    tanstack: { query: true },
  },
  backend: {
    stack: "koa",
    base: "/react-koa-ssr-tsq/api",
  },
  validation: true,
});
