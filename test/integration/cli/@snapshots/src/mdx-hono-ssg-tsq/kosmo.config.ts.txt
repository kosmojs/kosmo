import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "mdx",
    base: "/mdx-hono-ssg-tsq",
    fetch: true,
    ssr: true,
    ssg: true,
  },
  backend: {
    stack: "hono",
    base: "/mdx-hono-ssg-tsq/api",
  },
  validation: true,
});
