import {
  defineConfig,
  svelteGenerator,
  h3Generator,
  ssrGenerator,
  ssgGenerator,
  fetchGenerator,
  typeboxGenerator,
} from "@kosmojs/dev";

export default defineConfig({
  base: "/svelte-h3-ssg",
  generators: [
    svelteGenerator(),
    h3Generator(),
    ssrGenerator(),
    ssgGenerator(),
    fetchGenerator(),
    typeboxGenerator(),
  ],
});
