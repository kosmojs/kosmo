import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "mdx",
    base: "/mdx",
    ssr: true,
    ssg: false,
    tanstack: { query: false },
  },
  fetch: false,
  validation: false,
  typecheck: true,
});
