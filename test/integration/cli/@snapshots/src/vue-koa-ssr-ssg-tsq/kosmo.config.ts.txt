import {
  defineConfig,
  vueGenerator,
  koaGenerator,
  ssrGenerator,
  ssgGenerator,
  fetchGenerator,
  typeboxGenerator,
} from "@kosmojs/dev";

export default defineConfig({
  base: "/vue-koa-ssr-ssg-tsq",
  generators: [
    vueGenerator({ tanstack: { query: true } }),
    koaGenerator(),
    ssrGenerator(),
    ssgGenerator(),
    fetchGenerator(),
    typeboxGenerator(),
  ],
});
