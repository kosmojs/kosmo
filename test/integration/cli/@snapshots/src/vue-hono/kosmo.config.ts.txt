import {
  defineConfig,
  vueGenerator,
  honoGenerator,
  fetchGenerator,
  typeboxGenerator,
} from "@kosmojs/dev";

export default defineConfig({
  base: "/vue-hono",
  generators: [vueGenerator(), honoGenerator(), fetchGenerator(), typeboxGenerator()],
});
