import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "react",
    base: "/react-koa-ssr-ssg",
    fetch: true,
    ssr: true,
    ssg: true,
    tanstack: { query: false },
  },
  backend: {
    stack: "koa",
    base: "/react-koa-ssr-ssg/api",
  },
  validation: true,
});
