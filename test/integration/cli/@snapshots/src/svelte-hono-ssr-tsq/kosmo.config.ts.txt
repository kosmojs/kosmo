import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "svelte",
    base: "/svelte-hono-ssr-tsq",
    fetch: true,
    ssr: true,
    ssg: false,
    tanstack: { query: true },
  },
  backend: {
    stack: "hono",
    base: "/svelte-hono-ssr-tsq/api",
  },
  validation: true,
});
