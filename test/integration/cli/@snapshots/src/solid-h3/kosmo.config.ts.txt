import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "solid",
    base: "/solid-h3",
    ssr: true,
    ssg: false,
    tanstack: { query: false },
  },
  backend: {
    stack: "h3",
    base: "/solid-h3/api",
  },
  fetch: true,
  validation: true,
  typecheck: true,
});
