import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "react",
    base: "/react-h3-ssr-tsq",
    fetch: true,
    ssr: true,
    ssg: false,
    tanstack: { query: true },
  },
  backend: {
    stack: "h3",
    base: "/react-h3-ssr-tsq/api",
  },
  validation: true,
});
