import {
  defineConfig,
  reactGenerator,
  h3Generator,
  ssrGenerator,
  fetchGenerator,
  typeboxGenerator,
} from "@kosmojs/dev";

export default defineConfig({
  base: "/react-h3-ssr-tsq",
  generators: [
    reactGenerator({ tanstack: { query: true } }),
    h3Generator(),
    ssrGenerator(),
    fetchGenerator(),
    typeboxGenerator(),
  ],
});
