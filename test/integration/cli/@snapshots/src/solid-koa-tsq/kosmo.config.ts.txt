import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "solid",
    base: "/solid-koa-tsq",
    fetch: true,
    ssr: false,
    ssg: false,
    tanstack: { query: true },
  },
  backend: {
    stack: "koa",
    base: "/solid-koa-tsq/api",
  },
  validation: true,
});
