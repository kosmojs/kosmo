import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "solid",
    base: "/solid-hono-ssg-tsq",
    fetch: true,
    ssr: true,
    ssg: true,
    tanstack: { query: true },
  },
  backend: {
    stack: "hono",
    base: "/solid-hono-ssg-tsq/api",
  },
  validation: true,
});
