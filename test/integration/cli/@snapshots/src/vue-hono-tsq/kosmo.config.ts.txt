import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "vue",
    base: "/vue-hono-tsq",
    fetch: true,
    ssr: false,
    ssg: false,
    tanstack: { query: true },
  },
  backend: {
    stack: "hono",
    base: "/vue-hono-tsq/api",
  },
  validation: true,
});
