import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "solid",
    base: "/solid-tsq",
    fetch: true,
    ssr: false,
    ssg: false,
    tanstack: { query: true },
  },
});
