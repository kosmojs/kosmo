import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "vue",
    base: "/vue-koa-ssg",
    fetch: true,
    ssr: true,
    ssg: true,
    tanstack: { query: false },
  },
  backend: {
    stack: "koa",
    base: "/vue-koa-ssg/api",
  },
  validation: true,
});
