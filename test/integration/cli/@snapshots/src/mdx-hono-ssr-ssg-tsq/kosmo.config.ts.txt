import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "mdx",
    base: "/mdx-hono-ssr-ssg-tsq",
    fetch: true,
    ssr: true,
    ssg: true,
  },
  backend: {
    stack: "hono",
    base: "/mdx-hono-ssr-ssg-tsq/api",
  },
  validation: true,
});
