import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "mdx",
    base: "/mdx-hono-tsq",
    fetch: true,
    ssr: false,
    ssg: false,
  },
  backend: {
    stack: "hono",
    base: "/mdx-hono-tsq/api",
  },
  validation: true,
});
