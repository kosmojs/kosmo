import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "mdx",
    base: "/mdx-h3",
    fetch: true,
    ssr: false,
    ssg: false,
  },
  backend: {
    stack: "h3",
    base: "/mdx-h3/api",
  },
  validation: true,
});
