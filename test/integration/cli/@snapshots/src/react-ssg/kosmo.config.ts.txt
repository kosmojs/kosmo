import { defineConfig, reactGenerator, ssrGenerator, ssgGenerator } from "@kosmojs/dev";

export default defineConfig({
  base: "/react-ssg",
  generators: [reactGenerator(), ssrGenerator(), ssgGenerator()],
});
