import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "mdx",
    base: "/mdx-hono-ssr-tsq",
    fetch: true,
    ssr: true,
    ssg: false,
  },
  backend: {
    stack: "hono",
    base: "/mdx-hono-ssr-tsq/api",
  },
  validation: true,
});
