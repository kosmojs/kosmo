import {
  defineConfig,
  reactGenerator,
  h3Generator,
  ssrGenerator,
  ssgGenerator,
  fetchGenerator,
  typeboxGenerator,
} from "@kosmojs/dev";

export default defineConfig({
  base: "/react-h3-ssr-ssg",
  generators: [
    reactGenerator(),
    h3Generator(),
    ssrGenerator(),
    ssgGenerator(),
    fetchGenerator(),
    typeboxGenerator(),
  ],
});
