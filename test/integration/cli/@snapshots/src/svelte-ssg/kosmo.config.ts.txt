import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "svelte",
    base: "/svelte-ssg",
    fetch: true,
    ssr: true,
    ssg: true,
    tanstack: { query: false },
  },
});
