import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "mdx",
    base: "/mdx-koa-ssg",
    fetch: true,
    ssr: true,
    ssg: true,
  },
  backend: {
    stack: "koa",
    base: "/mdx-koa-ssg/api",
  },
  validation: true,
});
