import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "mdx",
    base: "/mdx-koa-ssr-ssg",
    fetch: true,
    ssr: true,
    ssg: true,
  },
  backend: {
    stack: "koa",
    base: "/mdx-koa-ssr-ssg/api",
  },
  validation: true,
});
