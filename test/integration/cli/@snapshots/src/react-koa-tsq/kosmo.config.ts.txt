import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "react",
    base: "/react-koa-tsq",
    fetch: true,
    ssr: false,
    ssg: false,
    tanstack: { query: true },
  },
  backend: {
    stack: "koa",
    base: "/react-koa-tsq/api",
  },
  validation: true,
});
