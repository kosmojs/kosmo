import {
  defineConfig,
  reactGenerator,
  honoGenerator,
  fetchGenerator,
  typeboxGenerator,
} from "@kosmojs/dev";

export default defineConfig({
  base: "/react-hono-tsq",
  generators: [
    reactGenerator({ tanstack: { query: true } }),
    honoGenerator(),
    fetchGenerator(),
    typeboxGenerator(),
  ],
});
