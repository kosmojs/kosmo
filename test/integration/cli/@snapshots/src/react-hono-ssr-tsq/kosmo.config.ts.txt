import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "react",
    base: "/react-hono-ssr-tsq",
    fetch: true,
    ssr: true,
    ssg: false,
    tanstack: { query: true },
  },
  backend: {
    stack: "hono",
    base: "/react-hono-ssr-tsq/api",
  },
  validation: true,
});
