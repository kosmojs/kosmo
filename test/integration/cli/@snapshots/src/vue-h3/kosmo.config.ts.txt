import {
  defineConfig,
  vueGenerator,
  h3Generator,
  fetchGenerator,
  typeboxGenerator,
} from "@kosmojs/dev";

export default defineConfig({
  base: "/vue-h3",
  generators: [vueGenerator(), h3Generator(), fetchGenerator(), typeboxGenerator()],
});
