import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "svelte",
    base: "/svelte-h3-ssr-ssg",
    fetch: true,
    ssr: true,
    ssg: true,
    tanstack: { query: false },
  },
  backend: {
    stack: "h3",
    base: "/svelte-h3-ssr-ssg/api",
  },
  validation: true,
});
