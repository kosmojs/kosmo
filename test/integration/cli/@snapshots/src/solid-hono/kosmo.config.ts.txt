import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "solid",
    base: "/solid-hono",
    fetch: true,
    ssr: false,
    ssg: false,
    tanstack: { query: false },
  },
  backend: {
    stack: "hono",
    base: "/solid-hono/api",
  },
  validation: true,
});
