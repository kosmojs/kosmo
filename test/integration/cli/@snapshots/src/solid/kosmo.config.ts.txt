import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "solid",
    base: "/solid",
    ssr: true,
    ssg: false,
    tanstack: { query: false },
  },
  fetch: false,
  validation: false,
  typecheck: true,
});
