import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "solid",
    base: "/solid",
    fetch: true,
    ssr: false,
    ssg: false,
    tanstack: { query: false },
  },
});
