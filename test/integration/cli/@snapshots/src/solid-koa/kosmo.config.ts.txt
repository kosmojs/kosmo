import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "solid",
    base: "/solid-koa",
    ssr: true,
    ssg: false,
    tanstack: { query: false },
  },
  backend: {
    stack: "koa",
    base: "/solid-koa/api",
  },
  fetch: true,
  validation: true,
  typecheck: true,
});
