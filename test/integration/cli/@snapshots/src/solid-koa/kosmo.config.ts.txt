import {
  defineConfig,
  solidGenerator,
  koaGenerator,
  fetchGenerator,
  typeboxGenerator,
} from "@kosmojs/dev";

export default defineConfig({
  base: "/solid-koa",
  generators: [solidGenerator(), koaGenerator(), fetchGenerator(), typeboxGenerator()],
});
