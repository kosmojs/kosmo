import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "vue",
    base: "/vue",
    fetch: true,
    ssr: false,
    ssg: false,
    tanstack: { query: false },
  },
});
