import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "vue",
    base: "/vue-h3-ssg-tsq",
    fetch: true,
    ssr: true,
    ssg: true,
    tanstack: { query: true },
  },
  backend: {
    stack: "h3",
    base: "/vue-h3-ssg-tsq/api",
  },
  validation: true,
});
