import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "vue",
    base: "/vue-hono-ssr-tsq",
    fetch: true,
    ssr: true,
    ssg: false,
    tanstack: { query: true },
  },
  backend: {
    stack: "hono",
    base: "/vue-hono-ssr-tsq/api",
  },
  validation: true,
});
