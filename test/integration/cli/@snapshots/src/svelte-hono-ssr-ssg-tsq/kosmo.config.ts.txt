import {
  defineConfig,
  svelteGenerator,
  honoGenerator,
  ssrGenerator,
  ssgGenerator,
  fetchGenerator,
  typeboxGenerator,
} from "@kosmojs/dev";

export default defineConfig({
  base: "/svelte-hono-ssr-ssg-tsq",
  generators: [
    svelteGenerator({ tanstack: { query: true } }),
    honoGenerator(),
    ssrGenerator(),
    ssgGenerator(),
    fetchGenerator(),
    typeboxGenerator(),
  ],
});
