import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "mdx",
    base: "/mdx-koa-ssr-tsq",
    fetch: true,
    ssr: true,
    ssg: false,
  },
  backend: {
    stack: "koa",
    base: "/mdx-koa-ssr-tsq/api",
  },
  validation: true,
});
