import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "mdx",
    base: "/mdx-koa-ssg-tsq",
    fetch: true,
    ssr: true,
    ssg: true,
  },
  backend: {
    stack: "koa",
    base: "/mdx-koa-ssg-tsq/api",
  },
  validation: true,
});
