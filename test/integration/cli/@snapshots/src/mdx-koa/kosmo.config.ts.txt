import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "mdx",
    base: "/mdx-koa",
    fetch: true,
    ssr: false,
    ssg: false,
  },
  backend: {
    stack: "koa",
    base: "/mdx-koa/api",
  },
  validation: true,
});
