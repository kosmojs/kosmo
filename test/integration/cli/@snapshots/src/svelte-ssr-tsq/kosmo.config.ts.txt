import { defineConfig, svelteGenerator, ssrGenerator } from "@kosmojs/dev";

export default defineConfig({
  base: "/svelte-ssr-tsq",
  generators: [svelteGenerator({ tanstack: { query: true } }), ssrGenerator()],
});
