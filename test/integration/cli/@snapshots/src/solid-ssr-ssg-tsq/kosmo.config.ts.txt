import { defineConfig, solidGenerator, ssrGenerator, ssgGenerator } from "@kosmojs/dev";

export default defineConfig({
  base: "/solid-ssr-ssg-tsq",
  generators: [solidGenerator({ tanstack: { query: true } }), ssrGenerator(), ssgGenerator()],
});
