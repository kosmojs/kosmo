import {
  defineConfig,
  vueGenerator,
  h3Generator,
  ssrGenerator,
  fetchGenerator,
  typeboxGenerator,
} from "@kosmojs/dev";

export default defineConfig({
  base: "/vue-h3-ssr-tsq",
  generators: [
    vueGenerator({ tanstack: { query: true } }),
    h3Generator(),
    ssrGenerator(),
    fetchGenerator(),
    typeboxGenerator(),
  ],
});
