import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "svelte",
    base: "/svelte-h3-ssr-ssg-tsq",
    fetch: true,
    ssr: true,
    ssg: true,
    tanstack: { query: true },
  },
  backend: {
    stack: "h3",
    base: "/svelte-h3-ssr-ssg-tsq/api",
  },
  validation: true,
});
