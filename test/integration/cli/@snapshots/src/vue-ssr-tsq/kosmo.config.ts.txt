import { defineConfig, vueGenerator, ssrGenerator } from "@kosmojs/dev";

export default defineConfig({
  base: "/vue-ssr-tsq",
  generators: [vueGenerator({ tanstack: { query: true } }), ssrGenerator()],
});
