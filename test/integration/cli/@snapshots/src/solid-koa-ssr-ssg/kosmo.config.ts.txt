import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "solid",
    base: "/solid-koa-ssr-ssg",
    fetch: true,
    ssr: true,
    ssg: true,
    tanstack: { query: false },
  },
  backend: {
    stack: "koa",
    base: "/solid-koa-ssr-ssg/api",
  },
  validation: true,
});
