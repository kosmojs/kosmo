import {
  defineConfig,
  reactGenerator,
  h3Generator,
  ssrGenerator,
  ssgGenerator,
  fetchGenerator,
  typeboxGenerator,
} from "@kosmojs/dev";

export default defineConfig({
  base: "/react-h3-ssr-ssg-tsq",
  generators: [
    reactGenerator({ tanstack: { query: true } }),
    h3Generator(),
    ssrGenerator(),
    ssgGenerator(),
    fetchGenerator(),
    typeboxGenerator(),
  ],
});
