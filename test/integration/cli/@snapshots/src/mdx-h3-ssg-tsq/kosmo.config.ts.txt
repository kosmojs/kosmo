import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "mdx",
    base: "/mdx-h3-ssg-tsq",
    fetch: true,
    ssr: true,
    ssg: true,
  },
  backend: {
    stack: "h3",
    base: "/mdx-h3-ssg-tsq/api",
  },
  validation: true,
});
