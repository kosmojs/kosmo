import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "solid",
    base: "/solid-koa-ssr-ssg-tsq",
    fetch: true,
    ssr: true,
    ssg: true,
    tanstack: { query: true },
  },
  backend: {
    stack: "koa",
    base: "/solid-koa-ssr-ssg-tsq/api",
  },
  validation: true,
});
