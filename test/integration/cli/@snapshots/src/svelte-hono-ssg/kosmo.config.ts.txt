import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "svelte",
    base: "/svelte-hono-ssg",
    fetch: true,
    ssr: true,
    ssg: true,
    tanstack: { query: false },
  },
  backend: {
    stack: "hono",
    base: "/svelte-hono-ssg/api",
  },
  validation: true,
});
