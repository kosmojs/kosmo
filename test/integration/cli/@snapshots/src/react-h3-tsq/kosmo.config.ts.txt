import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "react",
    base: "/react-h3-tsq",
    fetch: true,
    ssr: false,
    ssg: false,
    tanstack: { query: true },
  },
  backend: {
    stack: "h3",
    base: "/react-h3-tsq/api",
  },
  validation: true,
});
