import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
  frontend: {
    stack: "react",
    base: "/react-ssr-ssg",
    fetch: true,
    ssr: true,
    ssg: true,
    tanstack: { query: false },
  },
});
