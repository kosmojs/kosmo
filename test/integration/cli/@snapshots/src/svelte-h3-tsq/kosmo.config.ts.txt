import {
  defineConfig,
  svelteGenerator,
  h3Generator,
  fetchGenerator,
  typeboxGenerator,
} from "@kosmojs/dev";

export default defineConfig({
  base: "/svelte-h3-tsq",
  generators: [
    svelteGenerator({ tanstack: { query: true } }),
    h3Generator(),
    fetchGenerator(),
    typeboxGenerator(),
  ],
});
