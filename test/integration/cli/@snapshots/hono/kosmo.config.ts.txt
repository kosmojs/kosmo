import { defineConfig, honoGenerator, fetchGenerator, typeboxGenerator } from "@kosmojs/dev";

export default defineConfig({
  base: "/hono",
  generators: [honoGenerator(), fetchGenerator(), typeboxGenerator()],
});
