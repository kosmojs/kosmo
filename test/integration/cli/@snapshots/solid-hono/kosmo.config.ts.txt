import {
  defineConfig,
  solidGenerator,
  honoGenerator,
  fetchGenerator,
  typeboxGenerator,
} from "@kosmojs/dev";

export default defineConfig({
  base: "/solid-hono",
  generators: [solidGenerator(), honoGenerator(), fetchGenerator(), typeboxGenerator()],
});
