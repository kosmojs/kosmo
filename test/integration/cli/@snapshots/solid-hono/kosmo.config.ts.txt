import {
  defineConfig,
  solidGenerator,
  honoGenerator,
  fetchGenerator,
  typeboxGenerator,
} from "@kosmojs/dev";

import solidPlugin from "vite-plugin-solid";

export default defineConfig({
  base: {
    development: "/solid-hono",
  },
  plugins: [solidPlugin()],
  generators: [solidGenerator(), honoGenerator(), fetchGenerator(), typeboxGenerator()],
  oxc: { jsx: { importSource: "solid-js" } },
});
