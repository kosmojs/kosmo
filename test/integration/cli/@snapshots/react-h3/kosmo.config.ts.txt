import {
  defineConfig,
  reactGenerator,
  h3Generator,
  fetchGenerator,
  typeboxGenerator,
} from "@kosmojs/dev";

export default defineConfig({
  base: "/react-h3",
  generators: [reactGenerator(), h3Generator(), fetchGenerator(), typeboxGenerator()],
});
