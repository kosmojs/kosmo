import {
  defineConfig,
  vueGenerator,
  koaGenerator,
  fetchGenerator,
  typeboxGenerator,
} from "@kosmojs/dev";

export default defineConfig({
  base: "/vue-koa",
  generators: [vueGenerator(), koaGenerator(), fetchGenerator(), typeboxGenerator()],
});
