import { defineConfig, solidGenerator, ssrGenerator } from "@kosmojs/dev";

import solidPlugin from "vite-plugin-solid";

export default defineConfig({
  base: {
    development: "/solid-ssr",
  },
  plugins: [solidPlugin({ ssr: true })],
  generators: [solidGenerator(), ssrGenerator()],
  oxc: { jsx: { importSource: "solid-js" } },
});
