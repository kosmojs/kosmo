import {
  defineConfig,
  solidGenerator,
  koaGenerator,
  fetchGenerator,
  typeboxGenerator,
} from "@kosmojs/dev";

import solidPlugin from "vite-plugin-solid";

export default defineConfig({
  base: {
    development: "/solid-koa",
  },
  plugins: [solidPlugin()],
  generators: [solidGenerator(), koaGenerator(), fetchGenerator(), typeboxGenerator()],
  oxc: { jsx: { importSource: "solid-js" } },
});
