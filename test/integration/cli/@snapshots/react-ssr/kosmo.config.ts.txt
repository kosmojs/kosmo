import { defineConfig, reactGenerator, ssrGenerator } from "@kosmojs/dev";

import reactPlugin from "@vitejs/plugin-react";

export default defineConfig({
  base: {
    development: "/react-ssr",
  },
  plugins: [reactPlugin()],
  generators: [reactGenerator(), ssrGenerator()],
});
