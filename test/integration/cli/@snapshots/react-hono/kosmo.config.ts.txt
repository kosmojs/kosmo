import {
  defineConfig,
  reactGenerator,
  honoGenerator,
  fetchGenerator,
  typeboxGenerator,
} from "@kosmojs/dev";

import reactPlugin from "@vitejs/plugin-react";

export default defineConfig({
  base: "/react-hono",
  plugins: [reactPlugin()],
  generators: [reactGenerator(), honoGenerator(), fetchGenerator(), typeboxGenerator()],
});
