import {
  defineConfig,
  solidGenerator,
  honoGenerator,
  ssrGenerator,
  fetchGenerator,
  typeboxGenerator,
} from "@kosmojs/dev";

import solidPlugin from "vite-plugin-solid";

export default defineConfig({
  base: {
    development: "/solid-hono-ssr",
  },
  plugins: [solidPlugin({ ssr: true })],
  generators: [
    solidGenerator(),
    honoGenerator(),
    ssrGenerator(),
    fetchGenerator(),
    typeboxGenerator(),
  ],
  oxc: { jsx: { importSource: "solid-js" } },
});
