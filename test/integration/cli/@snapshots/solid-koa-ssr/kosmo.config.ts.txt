import {
  defineConfig,
  solidGenerator,
  koaGenerator,
  ssrGenerator,
  fetchGenerator,
  typeboxGenerator,
} from "@kosmojs/dev";

export default defineConfig({
  base: "/solid-koa-ssr",
  generators: [
    solidGenerator(),
    koaGenerator(),
    ssrGenerator(),
    fetchGenerator(),
    typeboxGenerator(),
  ],
});
