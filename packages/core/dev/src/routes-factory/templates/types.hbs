{{#each typeDeclarations}}{{text}}
{{/each}}

export type {{params.id}} = {
  {{#each paramsSchema}}
  "{{name}}"{{#unless isRequired}}?{{/unless}}:{{#if isSplat}} Array<{{/if}}
    {{#if refinement}}{{refinement.text}}{{else}}string{{/if}}
  {{#if isSplat}}>{{/if}}
  {{/each}}
};

{{#each validationTypes}}export type {{id}} = {{text}};
{{/each}}
