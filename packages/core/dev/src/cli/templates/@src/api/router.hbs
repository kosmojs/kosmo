import { routeStackBuilder } from "{{importPathmap.lib}}";

import createRouter from "{{importPathmap.core}}/router";

const router = createRouter();

for (const { name, path, methods, middleware } of routeStackBuilder()) {
  router.register(path, methods, middleware, { name });
}

export default router;
