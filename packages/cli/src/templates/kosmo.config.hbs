import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
{{#if frontend}}
  frontend: {
    {{#each options.frontend}}
    "{{@key}}": {{.}},
    {{/each}}
  },
{{/if}}
{{#if backend}}
  backend: {
    {{#each options.backend}}
    "{{@key}}": {{.}},
    {{/each}}
  },
  validation: true,
{{/if}}
});
