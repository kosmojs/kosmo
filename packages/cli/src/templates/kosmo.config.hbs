import { defineConfig } from "@kosmojs/dev";

export default defineConfig({
{{#if frontend}}
  frontend: {
    {{#each config.frontend}}
    "{{@key}}": {{.}},
    {{/each}}
  },
{{/if}}
{{#if backend}}
  backend: {
    {{#each config.backend}}
    "{{@key}}": {{.}},
    {{/each}}
  },
{{/if}}
{{#if sidecar}}
  sidecar: {
    {{#each config.sidecar}}
    "{{@key}}": {{.}},
    {{/each}}
  },
{{/if}}
{{#if config.fetch}}
  fetch: {{config.fetch}},
{{/if}}
{{#if config.validation}}
  validation: {{config.validation}},
{{/if}}
{{#if config.typecheck}}
  typecheck: {{config.typecheck}},
{{/if}}
});
