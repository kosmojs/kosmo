import {
  defineConfig,
  {{#each generators}}{{name}},
  {{/each}}
} from "@kosmojs/dev";

{{#each imports}}{{.}}
{{/each}}

{{#each plugins}}{{importDeclaration}}
{{/each}}

export default defineConfig({
  base: {
    development: "{{base}}",
  },
  plugins: [
    {{#each plugins}}{{importName}}({{options}}),
    {{/each}}
  ],
  generators: [
    {{#each generators}}{{name}}({{options}}),
    {{/each}}
  ],
  {{#each frameworkSpecificOptions}}{{.}}
  {{/each}}
});
