import {
  defineConfig,
{{#each generators}}  {{name}},
{{/each}}
} from "@kosmojs/dev";

{{#each imports}}{{.}}
{{/each}}
export default defineConfig({
  base: "{{base}}",
  generators: [
{{#each generators}}    {{name}}({{options}}),
{{/each}}
  ],
});
