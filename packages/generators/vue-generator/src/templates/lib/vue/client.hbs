
{{#each pageEntries}}
const {{importName}} = () => import("{{importPath .}}");
{{/each}}

export const routeStackBuilder = () => {
  return [
    {{#each nestedRoutes}}{{> routePartial}},
    {{/each}}
  ];
}

export const shouldHydrate = () => {{shouldHydrate}};
