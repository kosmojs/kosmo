
{{#each pageEntries}}
import {{importName}} from "{{importPath .}}";
{{/each}}

export const routeStackBuilder = () => {
  return [
    {{#each nestedRoutes}}{{> routePartial}},
    {{/each}}
  ];
}
