import { defineRoute } from "{{ createImport "libApi" route.name }}";

export default defineRoute(({ GET }) => [
  GET(async (ctx) => {
    // Always `return` the response!
    // ❗ Never call `ctx.json()` / `ctx.text()` / `ctx.body()` without returning!
    return ctx.text("Automatically generated route: [ {{route.name}} ]");
  }),
]);
