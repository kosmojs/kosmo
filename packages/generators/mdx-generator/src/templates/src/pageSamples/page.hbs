---
title: "{{title}}"
---

import PageSample from "{{ createImport "lib" "pageSamples/page.tsx" }}";

export const pathMap = {
  page: "{{ createImport "pages" route.file }}",
  pages: "{{ createImport "pages" }}",
  api: "{{ createImport "api" }}",
};

<PageSample
  message="{{message}}"
  routeName="{{route.name}}"
  pathMap={pathMap}
/>
