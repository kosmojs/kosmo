import { use } from "@kosmojs/api";

export default [
  use(async function {{funcName}}(ctx, next) {
    // Always `return next()` or `await next()`; 🚫 Never call `next()` by itself!
    return next();
  }),
];
