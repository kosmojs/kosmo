{
  "type": "module",
  "private": true,
  "packageManager": "pnpm@11.23.0",
  "devEngines": {
    "runtime": {
      "name": "node",
      "version": "^24.16.0"
    }
  },
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "build": "pnpm -r --filter '!docs' build",
    "test": "vitest run",
    "test:unit": "vitest run --project=packages/* --project=generators/*",
    "test:unit:generators": "vitest run --project=generators/*",
    "test:unit:packages": "vitest run --project=packages/*",
    "test:integration": "vitest run --project=\"integration:*\"",
    "test:integration:cli": "vitest run --project=integration:cli",
    "test:integration:backend": "vitest run --project=integration:backend",
    "test:integration:csr": "vitest run --project=integration:csr",
    "test:integration:ssr": "vitest run --project=integration:ssr",
    "test:integration:ssg": "vitest run --project=integration:ssg",
    "release:prepare": "bash ./scripts/release/prepare",
    "release:commit": "bash ./scripts/release/commit"
  },
  "dependencies": {
    "@biomejs/biome": "^2.5.12",
    "@changesets/cli": "^3.0.2",
    "@types/bun": "^1.4.2",
    "@types/deno": "^2.7.0",
    "@types/node": "^26.5.0",
    "conventional-changelog": "^8.1.3",
    "conventional-changelog-conventionalcommits": "^10.4.0",
    "tsconfig": "workspace:^",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0",
    "wsbuild": "workspace:^"
  }
}
